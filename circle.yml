machine:
  node:
    version: 5.12.0

dependencies:
  override:
    - sudo apt-get install -y expect lftp
    - npm install -g gulp bower
    - npm install
    - bower install

deployment:
  staging:
    owner: votedotorg
    branch: test-circle
    commands:
      - mkdir bower_components
      - gulp --production